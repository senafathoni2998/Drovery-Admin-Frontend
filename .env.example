# Base URL of the drovery-backend API (includes the /api/v1 prefix).
# The backend's CORS defaults to '*' when CORS_ORIGINS is unset, so this works in dev.
VITE_API_BASE_URL=http://localhost:3000/api/v1
